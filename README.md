# VeloFX
A lightweight, high-performance C++20 closed-source SDK wrapper for ExchangeRate-API (v6). Delivered as a pre-compiled static library for Windows (MSVC x64), it features a file-based binary caching layer to eliminate network latency and safeguard API quotas.
